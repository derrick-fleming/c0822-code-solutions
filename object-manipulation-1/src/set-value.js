/* exported setValue */

/* 1. Define the functiont to take three parameters: object, key, value
2. Add the key & value parameter to the original object by:
     - Assigning the value to the object's key using bracket notation
3.  Return nothing. */

function setValue(object, key, value) {
  object[key] = value;
}
