/* exported getFirstChar */
/* 1. Create and define the function that we're going to use to get first character;
2. Create a container to store the string's first character value;
3. Set that container equal to the string[0] to get the first character;
4. Return the value from the function. */

function getFirstChar(string) {
  var firstChar = string[0];
  return firstChar;
}
