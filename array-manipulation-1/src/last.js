/* exported last */

/* 1. Set the array parameter to be the array.length - 1 to get to the last element
2. Return the value of that parameter from the function. */

function last(array) {
  return array[array.length - 1];
}
